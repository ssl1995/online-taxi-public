server:
  port: 9000

spring:
  application:
    name: service-sse-push
  cloud:
    nacos:
      discovery:
        server-addr: http://101.201.154.144:8848

management:
  endpoint:
    web:
      exposure:
        include: '*'
